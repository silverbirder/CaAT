describe('MemberImpl', ()=> {
    describe('fetchSchedules', () => {
        test('None', () => {
            // const member: IMember = new MemberImpl('user@gmail.com');
            // const schedules: Array<ISchedule> = member.fetchSchedules();
            // TODO: DIP (CalendarApp.getCalendarById)
        })
    })
});